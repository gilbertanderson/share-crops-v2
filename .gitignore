node_modules
dist
dist-ssr
*.local
.env.local
.DS_Store

# Firebase service-account keys — NEVER commit. Real private keys.
*firebase-adminsdk*.json
serviceAccount*.json
.vscode/*
!.vscode/extensions.json
.vercel
.vercel/output
# Bundled Vercel serverless function — generated by scripts/build-api.mjs during
# Vercel deploy (see vercel.json buildCommand). Not published on Netlify; keeping
# it out of git avoids Netlify secret-scan false positives in SDK docstrings.
api/index.js

# Test artifacts
test-results
.playwright/
supabase/.temp

# Firebase emulator logs
*-debug.log
firebase-debug.log
.env*
